You will need a `.env` at the base project level that has the root password under a variable named `AUTH`. 
This is needed to run the following command. 
```
sudo systemctl restart firmwared.service
```